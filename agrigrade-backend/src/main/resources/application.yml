spring:
  profiles:
    active: dev
  application:
    name: agrigrade-backend

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
